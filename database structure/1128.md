```cpp
//sort.h
int FM(int, int*);

//sort.cpp
#include "stdio.h"
#include "stdlib.h"
#include "sort.h"

int main(){
	int A[10];
	int x, i;
	
	for(int i=0; i <10; i++){
		printf("請輸入第%d個數:", i+1);
		scanf("%d", &A[i]);
	}
	printf("顯示這10個數:\n");
	for(int i=0; i <10; i++){
		printf("%d, ", A[i]);
	}
	printf("\n");
	
	for(int i=0; i <10; i++){
		FM(i, A);
	}
	
	printf("顯示這10個數:\n");
	for(int i=0; i <10; i++){
		printf("%d, ", A[i]);
	}
	return 1;
}

int FM(int i, int A[10]){
	int k,y=i;
	for(int m=i;m <10; m++){
		if(A[m] > A[y]) y=m;
	}
	k = A[i];
	A[i] = A[y];
	A[y] = k; 
	return 1;
}

```
```cpp
int FM(int,int*)

#include "stdio.h"
#include "stdlib.h"

int main(){
	int A[10];
	int x, i;
	
	for(int i=0; i <10; i++){
		printf("請輸入第%d個數:", i+1);
		scanf("%d", &A[i]);
	}
	printf("顯示這10個數:\n");
	for(int i=0; i <10; i++){
		printf("%d, ", A[i]);
	}
	printf("\n");
	
	for(int i=0; i <10; i++){
		FM(i, A);
	}
	
	printf("顯示這10個數:\n");
	for(int i=0; i <10; i++){
		printf("%d, ", A[i]);
	}
	return 1;
}

int FM(int i, int A[10]){
	int k,y;
	for(int y; y<10; y++){
		for(int i; i<10; i++){
			if(A[i]>A[i+1])
			k = A[i];
			A[i] = A[i+1];
			A[y] = k; 
			}
	}
return 1;
}
```
