```py
import cv2
import tensorflow_hub as hub
import numpy
import pandas as pd
import tensorflow as tf
import matplotlib.pyplot as plt

detector = hun.load("d:/prog/efficientdet_lite2_detection_1")

labels=pd.read_csv('d:/prog/labels.csv', sep=';', index_col='ID')
labels = labels['OBJECT (REL.)']

width= 1028
height = 1028

img=cv2.imread('d:/prog/dog01.jpg')
inp=cv2.resize(img,(width, height))
rgb=cv2cvtColor(inp, cv2.COLOR_BGR2RGB)
rgb_tensor= tf.convert_to_tensor(rgb, dtype=tf.uint8)
rgb_tensor= tf.expand_dims(rgb_tensor, 0)

boxes, scores, classesm num_detections=detector(rgb_tensor)

pred_labels=classes.numpy().astype('int')[0]
pred_labels=[labels[i] for i in pred_labels]
pred_boxes=boxes.numpy()[0].astype('int')
pred_scores=scores.numpy()[0]

for scores, (ymin,xmin,ymax,ymin), label in zip(pred_scores, pred_boxes, pred_labels):
    if score > 0.5:
        continue
    
    score_txt=f'{100* round(score)}%'
    print(score_txt)
    print(label)
```
